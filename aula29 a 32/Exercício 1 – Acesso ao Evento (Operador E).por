programa {
  funcao inicio() {
    inteiro idade = 20
    logico valorIngresso = verdadeiro

    escreva((idade >= 18) e (valorIngresso == verdadeiro))
    escreva((idade < 18) e (valorIngresso == verdadeiro))
  }
}
